# Server configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Qdisc execution mode:
# - stub: do not execute system commands, just simulate success (default)
# - exec: reserved for future actual execution
QDISC_EXEC_MODE=stub

# Directory for JSON persistence of qdisc definitions
DATA_DIR=./data
